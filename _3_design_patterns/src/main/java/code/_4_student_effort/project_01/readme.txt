An online store application has products that can be listed/filtered by price in two ways:
1) ascending order;
2) descending order.

The project is using 3 design pattern:
1) Singleton Pattern - to ensure one instance of online store application class running on a machine;
2) Factory Pattern - to create different sub-classes of class Product;
3) Strategy Pattern - to define order algorithms for the products of the online store.